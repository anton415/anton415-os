| Документ | Где лежит | О чем | Актуален? | Как проверить | Что сделать |
| --- | --- | --- | --- | --- | --- |
| Pull request template | `.github/pull_request_template.md` | Шаблон PR: issue, intent, scope, review gate, sensitive areas, verification, release notes. | Да | Сверить с `.github/ISSUE_TEMPLATE/*.yml`, `docs/github-feature-ritual.md`, `Makefile`. | Оставить; обновлять только если меняется review/release workflow или набор обязательных проверок. |
| Changelog | `CHANGELOG.md` | История релизов и Unreleased-изменения по Todo, Finance и Platform. | Частично | Проверить через `gh release list --repo anton415/anton415-hub --limit 20`, `git log`, закрытые issue/PR. | Синхронизировать Unreleased с текущим release target: `v0.2.1` уже latest release от 2026-05-04, поэтому следующая цель должна быть новой. |
| README | `README.md` | Главная карта продукта: статус production, модули, архитектура, локальный запуск, CI/CD, ops, ссылки на docs. | Частично | Сверить модули с `find internal -maxdepth 3 -type d`, routes с `apps/web/src/types.ts` и `internal/platform/http/router.go`, команды с `make -n ...`, релизы с `gh release list`. | Обновить release-секцию: `v0.2.1` уже выпущен; добавить актуальную следующую цель и, при необходимости, текущие Todo hierarchy/recurrence возможности. |
| Deploy assets | `deploy/README.md` | Production compose, Caddy, backup script, миграции, Lockbox env-файлы и ручное использование deploy assets. | Да | Сверить с `deploy/docker-compose.production.yml`, `deploy/migrate-production.sh`, `deploy/backup/pg_dump_to_object_storage.sh`, `.github/workflows/deploy.yml`. | Оставить; обновлять вместе с изменениями production compose, backup path, миграций или Lockbox sync. |
| Architecture | `docs/architecture.md` | Архитектурное направление: Go modular monolith, bounded contexts, auth boundary, dependency rules, module boundaries. | Частично | Сверить с `internal/*`, `internal/platform/http/router.go`, `migrations/*.up.sql`, `apps/web/src/modules.ts`. | Обновить устаревшие формулировки про Step 3/Todo v1 и Finance exclusions: код уже содержит Todo archive/delete, hierarchy, URL, recurrence, priorities, flags и Finance settings/limits. |
| Dependency Updates | `docs/dependency-updates.md` | Политика ручных dependency refresh и security updates. | Да | Сверить с `.github/dependabot.yml`, `go.mod`, `apps/web/package.json`, `Makefile`, CI workflow. | Оставить; обновлять при смене Dependabot policy, toolchain или release verification set. |
| Redesign v1 | `docs/design/anton415-hub-redesign-v1.md` | Первая дизайн-дирекция: спокойный dense dashboard, Figma handoff, screen requirements, states, responsive rules. | Частично | Сверить с `docs/design/anton415-hub-redesign-v2.md`, `apps/web/src/render.ts`, `apps/web/src/styles.css`, Playwright smoke tests. | Явно пометить как исторический дизайн-референс или superseded by v2, чтобы не конкурировал с текущей v2-дирекцией. |
| Redesign v2 | `docs/design/anton415-hub-redesign-v2.md` | Текущая UI-дирекция Daily Center: launcher rail, action bar, work panels, spreadsheet-like finance, QA notes. | Частично | Сверить с `apps/web/src/render.ts`, `apps/web/src/styles.css`, `apps/web/tests/*.spec.ts`, визуальной проверкой desktop/mobile. | Оставить как текущий дизайн-контракт, но повторить и датировать QA notes после последних Todo/Finance изменений. |
| Development Setup | `docs/dev-setup.md` | Локальный запуск, env vars, команды, БД, integration smoke, troubleshooting. | Частично | Сверить команды через `make -n dev api web db stop test test-e2e test-integration lint build docker-build migrate-up migrate-down docker-config go-mod-tidy`, БД через `migrations/*.up.sql`, env через `.env.example` и `internal/platform/config/config.go`. | Обновить секцию Database: сейчас описаны только ранние Todo-таблицы, а фактическая БД включает `platform_metadata`, `auth_*`, Todo lifecycle/hierarchy fields, `finance_monthly_*`, `finance_settings`, `finance_expense_limit_settings`; добавить недостающие команды `docker-build` и `go-mod-tidy`. |
| GitHub Actions | `docs/github-actions.md` | CI и Deploy Production: secrets, variables, temporary SSH ingress, image build/push/deploy flow. | Да | Сверить с `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`, `infra/terraform/README.md`, deploy scripts. | Оставить; обновлять при смене GitHub secrets/variables, Yandex permissions или deploy workflow. |
| GitHub Feature Ritual | `docs/github-feature-ritual.md` | Процесс feature issues, project stages, Draft PR, Codex review gate, release, monitoring. | Да | Сверить с `.github/ISSUE_TEMPLATE/*.yml`, `.github/pull_request_template.md`, открытыми issue через `gh issue list --repo anton415/anton415-hub --state open`. | Оставить; обновлять при изменении GitHub Project fields, labels, review gate или release monitoring rules. |
| Migration Policy | `docs/migration.md` | Роли репозиториев, source-of-truth правила, что можно и нельзя переносить из legacy repos. | Да | Сверить с `README.md`, `docs/roadmap.md`, текущим remote `anton415/anton415-hub`, legacy repo references. | Оставить; пересмотреть только при реальной controlled extraction или смене source-of-truth репозитория. |
| Production Runbook | `docs/production.md` | Production runtime, secrets, OAuth callbacks, Yandex setup, SSH, deploy, rename cutover, backups, smoke checks. | Частично | Сверить с `Dockerfile`, `deploy/docker-compose.production.yml`, `.github/workflows/deploy.yml`, `deploy/backup/pg_dump_to_object_storage.sh`, `migrations/*.up.sql`. | Исправить формулировки, где runtime/backups описывают только Todo/auth: Finance тоже хранится в той же PostgreSQL и должен входить в restore/post-release checks. |
| Roadmap | `docs/roadmap.md` | V1 цель, constraints, repository strategy, implementation sequence, near-term releases, non-goals. | Нет/устарел | Сверить с `migrations/*.up.sql`, `internal/todo`, `internal/finance`, `CHANGELOG.md`, `gh release list`, `gh issue list --repo anton415/anton415-hub --state open`. | Переписать near-term sequence: Todo lifecycle/hierarchy, URL, archive, recurrence и Finance polish уже сделаны; `v0.2.1` уже latest release; строка "No Yandex Cloud deployment scripts yet" неверна; реальные открытые задачи сейчас #45, #46, #49, #50. |
| Yandex Cost Estimate | `docs/yandex-cost-estimate.md` | Предварительная оценка стоимости Yandex Cloud на 2026-04-26. | Нет/устарел | Сверить с актуальными Yandex Cloud прайсами, `infra/terraform/*.tf`, production sizing и billing dashboard. | Пересчитать после изменения цен от 2026-05-01 или явно пометить документ как исторический pre-apply estimate. |
| Terraform README | `infra/terraform/README.md` | Production Terraform stack: ресурсы, apply, SSH access, Lockbox, DNS, backup drill. | Да | Сверить с `infra/terraform/*.tf`, `infra/terraform/cloud-init.yaml.tftpl`, `deploy/README.md`, `.github/workflows/deploy.yml`. | Оставить; обновлять при изменении Terraform resources, variables, cloud-init, Lockbox или backup drill. |
